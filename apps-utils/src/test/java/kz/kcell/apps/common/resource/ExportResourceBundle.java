package kz.kcell.apps.common.resource;

import lombok.extern.slf4j.Slf4j;

/**
 * @author Pavel.Terechshenkov@kcell.kz
 * @since 14 09 2015
 */
@Slf4j
public class ExportResourceBundle {

/*
    @Test
    public void exportAsInsertStatement() throws InterruptedException {
        Thread.sleep(3000l);

        Arrays.asList(ResourceBundle.values()).forEach(
                r -> {
                    System.out.println(String.format(" Insert into APP_SPMOT_DB.STRING_RESOURCES (LANG,KEY,VALUE) values ('%s','%s',\t'%s');", "RU", r.name(), $(Language.RU, r).replace("'", "''")));
                }
        );


        Arrays.asList(((Enum)ResourceBundle).values()).forEach(
                r -> {
                    System.out.println(String.format(" Insert into APP_SPMOT_DB.STRING_RESOURCES (LANG,KEY,VALUE) values ('%s','%s',\t'%s');", "KK", r.name(), $(Language.KK, r).replace("'", "''")));
                }
        );

    }
*/
}
