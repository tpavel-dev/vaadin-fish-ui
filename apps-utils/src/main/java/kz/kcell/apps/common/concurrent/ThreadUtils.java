/*
package kz.kcell.apps.common.concurrent;

*/
/**
 * @author Pavel.Terechshenkov@kcell.kz
 * @since 13 04 2016
 *//*

public class ThreadUtils {
    */
/**
     * Extend thread name.
     * Add suffix into end name
     * @param nameSuffix
     *  add to end name of thread
     * @return old name thread
     *//*

    public static String extendThreadName(String nameSuffix) {
        final Thread currentThread = Thread.currentThread();
        final String oldName = currentThread.getName();
        currentThread.setName(oldName+nameSuffix);
        return oldName;
    }

    public static void clearThreadName() {
        final Thread currentThread = Thread.currentThread();
        currentThread.setName("");
    }

}
*/
